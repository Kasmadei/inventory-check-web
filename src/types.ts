export enum Routes {
  home = "/",
  overview = "/overview",
  inventory = "/inventory",
  details = "/details",
  scanners = "/scanners",
  splitAssembly = "/splitAssembly",
  locationAndSection = "/locationAndSection",
  control = "/control",
  readers = "/readers",
  attendance = "/attendance"
}
